import styled from 'styled-components'
import { flexSet } from '@common/styles/theme.styles'

const Title = styled.h1`
  ${flexSet('center', 'center')}
  color: red;
  font-size: 50px;
`
const s = {
  Title,
}

export default s
